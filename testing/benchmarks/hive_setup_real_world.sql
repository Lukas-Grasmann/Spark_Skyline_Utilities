-- noinspection SqlNoDataSourceInspectionForFile

CREATE DATABASE IF NOT EXISTS benchmarks;

USE benchmarks;

CREATE TABLE IF NOT EXISTS airbnb (
    id INT,
    accommodates INT,
    bedrooms INT,
    beds INT,
    price DOUBLE,
    number_of_reviews DOUBLE,
    review_scores_rating DOUBLE,
    review_scores_accuracy DOUBLE,
    review_scores_cleanliness DOUBLE,
    review_scores_checkin DOUBLE,
    review_scores_communication DOUBLE,
    review_scores_location DOUBLE,
    review_scores_value DOUBLE
) ROW FORMAT DELIMITED FIELDS TERMINATED BY ','
tblproperties("skip.header.line.count"="1");

CREATE TABLE IF NOT EXISTS fueleconomy (
    make STRING,
    model STRING,
    cylinders INT,
    displ DOUBLE,
    combinedCD DOUBLE,
    fuelCost08 INT,
    comb08 INT,
    barrels08 DOUBLE,
    city08 INT,
    cityUF DOUBLE,
    highway08 INT
) ROW FORMAT DELIMITED FIELDS TERMINATED BY ','
tblproperties("skip.header.line.count"="1");

CREATE TABLE IF NOT EXISTS coil2000 (
    ID INT,
    MOSTYPE INT,
    MAANTHUI INT,
    MGEMOMV INT,
    MGEMLEEF INT,
    MOSHOOFD INT,
    MGODRK INT,
    MGODPR INT,
    MGODOV INT,
    MGODGE INT,
    MRELGE INT,
    MRELSA INT,
    MRELOV INT,
    MFALLEEN INT,
    MFGEKIND INT,
    MFWEKIND INT,
    MOPLHOOG INT,
    MOPLMIDD INT,
    MOPLLAAG INT,
    MBERHOOG INT,
    MBERZELF INT,
    MBERBOER INT,
    MBERMIDD INT,
    MBERARBG INT,
    MBERARBO INT,
    MSKA INT,
    MSKB1 INT,
    MSKB2 INT,
    MSKC INT,
    MSKD INT,
    MHHUUR INT,
    MHKOOP INT,
    MAUT1 INT,
    MAUT2 INT,
    MAUT0 INT,
    MZFONDS INT,
    MZPART INT,
    MINKM30 INT,
    MINK3045 INT,
    MINK4575 INT,
    MINK7512 INT,
    MINK123M INT,
    MINKGEM INT,
    MKOOPKLA INT,
    PWAPART INT,
    PWABEDR INT,
    PWALAND INT,
    PPERSAUT INT,
    PBESAUT INT,
    PMOTSCO INT,
    PVRAAUT INT,
    PAANHANG INT,
    PTRACTOR INT,
    PWERKT INT,
    PBROM INT,
    PLEVEN INT,
    PPERSONG INT,
    PGEZONG INT,
    PWAOREG INT,
    PBRAND INT,
    PZEILPL INT,
    PPLEZIER INT,
    PFIETS INT,
    PINBOED INT,
    PBYSTAND INT,
    AWAPART INT,
    AWABEDR INT,
    AWALAND INT,
    APERSAUT INT,
    ABESAUT INT,
    AMOTSCO INT,
    AVRAAUT INT,
    AAANHANG INT,
    ATRACTOR INT,
    AWERKT INT,
    ABROM INT,
    ALEVEN INT,
    APERSONG INT,
    AGEZONG INT,
    AWAOREG INT,
    ABRAND INT,
    AZEILPL INT,
    APLEZIER INT,
    AFIETS INT,
    AINBOED INT,
    ABYSTAND INT,
    CARAVAN INT
) ROW FORMAT DELIMITED FIELDS TERMINATED BY ','
tblproperties("skip.header.line.count"="1");

CREATE TABLE IF NOT EXISTS nba (
   PLAYER_ID INT,
   PLAYER_NAME STRING,
   NICKNAME STRING,
   TEAM_ID INT,
   TEAM_ABBREVIATION STRING,
   AGE INT,
   GP INT,
   W INT,
   L INT,
   W_PCT DOUBLE,
   MIN DOUBLE,
   FGM DOUBLE,
   FGA DOUBLE,
   FG_PCT DOUBLE,
   FG3M DOUBLE,
   FG3A DOUBLE,
   FG3_PCT DOUBLE,
   FTM DOUBLE,
   FTA DOUBLE,
   FT_PCT DOUBLE,
   OREB DOUBLE,
   DREB DOUBLE,
   REB DOUBLE,
   AST DOUBLE,
   TOV DOUBLE,
   STL DOUBLE,
   BLK DOUBLE,
   BLKA DOUBLE,
   PF DOUBLE,
   PFD DOUBLE,
   PTS DOUBLE,
   PLUS_MINUS DOUBLE,
   NBA_FANTASY_PTS DOUBLE,
   DD2 INT,
   TD3 INT,
   GP_RANK INT,
   W_RANK INT,
   L_RANK INT,
   W_PCT_RANK INT,
   MIN_RANK INT,
   FGM_RANK INT,
   FGA_RANK INT,
   FG_PCT_RANK INT,
   FG3M_RANK INT,
   FG3A_RANK INT,
   FG3_PCT_RANK INT,
   FTM_RANK INT,
   FTA_RANK INT,
   FT_PCT_RANK INT,
   OREB_RANK INT,
   DREB_RANK INT,
   REB_RANK INT,
   AST_RANK INT,
   TOV_RANK INT,
   STL_RANK INT,
   BLK_RANK INT,
   BLKA_RANK INT,
   PF_RANK INT,
   PFD_RANK INT,
   PTS_RANK INT,
   PLUS_MINUS_RANK INT,
   NBA_FANTASY_PTS_RANK INT,
   DD2_RANK INT,
   TD3_RANK INT,
   CFID INT,
   CFPARAMS BIGINT
) ROW FORMAT DELIMITED FIELDS TERMINATED BY ','
tblproperties("skip.header.line.count"="1");

CREATE TABLE IF NOT EXISTS airbnb_incomplete (
    id INT,
    accommodates INT,
    bedrooms INT,
    beds INT,
    price DOUBLE,
    number_of_reviews DOUBLE,
    review_scores_rating DOUBLE,
    review_scores_accuracy DOUBLE,
    review_scores_cleanliness DOUBLE,
    review_scores_checkin DOUBLE,
    review_scores_communication DOUBLE,
    review_scores_location DOUBLE,
    review_scores_value DOUBLE
) ROW FORMAT DELIMITED FIELDS TERMINATED BY ','
tblproperties("skip.header.line.count"="1");

CREATE TABLE IF NOT EXISTS fueleconomy_incomplete (
    make STRING,
    model STRING,
    cylinders INT,
    displ DOUBLE,
    combinedCD DOUBLE,
    fuelCost08 INT,
    comb08 INT,
    barrels08 DOUBLE,
    city08 INT,
    cityUF DOUBLE,
    highway08 INT
) ROW FORMAT DELIMITED FIELDS TERMINATED BY ','
tblproperties("skip.header.line.count"="1");

CREATE TABLE IF NOT EXISTS coil2000_incomplete (
    ID INT,
    MOSTYPE INT,
    MAANTHUI INT,
    MGEMOMV INT,
    MGEMLEEF INT,
    MOSHOOFD INT,
    MGODRK INT,
    MGODPR INT,
    MGODOV INT,
    MGODGE INT,
    MRELGE INT,
    MRELSA INT,
    MRELOV INT,
    MFALLEEN INT,
    MFGEKIND INT,
    MFWEKIND INT,
    MOPLHOOG INT,
    MOPLMIDD INT,
    MOPLLAAG INT,
    MBERHOOG INT,
    MBERZELF INT,
    MBERBOER INT,
    MBERMIDD INT,
    MBERARBG INT,
    MBERARBO INT,
    MSKA INT,
    MSKB1 INT,
    MSKB2 INT,
    MSKC INT,
    MSKD INT,
    MHHUUR INT,
    MHKOOP INT,
    MAUT1 INT,
    MAUT2 INT,
    MAUT0 INT,
    MZFONDS INT,
    MZPART INT,
    MINKM30 INT,
    MINK3045 INT,
    MINK4575 INT,
    MINK7512 INT,
    MINK123M INT,
    MINKGEM INT,
    MKOOPKLA INT,
    PWAPART INT,
    PWABEDR INT,
    PWALAND INT,
    PPERSAUT INT,
    PBESAUT INT,
    PMOTSCO INT,
    PVRAAUT INT,
    PAANHANG INT,
    PTRACTOR INT,
    PWERKT INT,
    PBROM INT,
    PLEVEN INT,
    PPERSONG INT,
    PGEZONG INT,
    PWAOREG INT,
    PBRAND INT,
    PZEILPL INT,
    PPLEZIER INT,
    PFIETS INT,
    PINBOED INT,
    PBYSTAND INT,
    AWAPART INT,
    AWABEDR INT,
    AWALAND INT,
    APERSAUT INT,
    ABESAUT INT,
    AMOTSCO INT,
    AVRAAUT INT,
    AAANHANG INT,
    ATRACTOR INT,
    AWERKT INT,
    ABROM INT,
    ALEVEN INT,
    APERSONG INT,
    AGEZONG INT,
    AWAOREG INT,
    ABRAND INT,
    AZEILPL INT,
    APLEZIER INT,
    AFIETS INT,
    AINBOED INT,
    ABYSTAND INT,
    CARAVAN INT
) ROW FORMAT DELIMITED FIELDS TERMINATED BY ','
tblproperties("skip.header.line.count"="1");

CREATE TABLE IF NOT EXISTS nba_incomplete (
    PLAYER_ID INT,
   PLAYER_NAME STRING,
   NICKNAME STRING,
   TEAM_ID INT,
   TEAM_ABBREVIATION STRING,
   AGE INT,
   GP INT,
   W INT,
   L INT,
   W_PCT DOUBLE,
   MIN DOUBLE,
   FGM DOUBLE,
   FGA DOUBLE,
   FG_PCT DOUBLE,
   FG3M DOUBLE,
   FG3A DOUBLE,
   FG3_PCT DOUBLE,
   FTM DOUBLE,
   FTA DOUBLE,
   FT_PCT DOUBLE,
   OREB DOUBLE,
   DREB DOUBLE,
   REB DOUBLE,
   AST DOUBLE,
   TOV DOUBLE,
   STL DOUBLE,
   BLK DOUBLE,
   BLKA DOUBLE,
   PF DOUBLE,
   PFD DOUBLE,
   PTS DOUBLE,
   PLUS_MINUS DOUBLE,
   NBA_FANTASY_PTS DOUBLE,
   DD2 INT,
   TD3 INT,
   GP_RANK INT,
   W_RANK INT,
   L_RANK INT,
   W_PCT_RANK INT,
   MIN_RANK INT,
   FGM_RANK INT,
   FGA_RANK INT,
   FG_PCT_RANK INT,
   FG3M_RANK INT,
   FG3A_RANK INT,
   FG3_PCT_RANK INT,
   FTM_RANK INT,
   FTA_RANK INT,
   FT_PCT_RANK INT,
   OREB_RANK INT,
   DREB_RANK INT,
   REB_RANK INT,
   AST_RANK INT,
   TOV_RANK INT,
   STL_RANK INT,
   BLK_RANK INT,
   BLKA_RANK INT,
   PF_RANK INT,
   PFD_RANK INT,
   PTS_RANK INT,
   PLUS_MINUS_RANK INT,
   NBA_FANTASY_PTS_RANK INT,
   DD2_RANK INT,
   TD3_RANK INT,
   CFID INT,
   CFPARAMS BIGINT
) ROW FORMAT DELIMITED FIELDS TERMINATED BY ','
tblproperties("skip.header.line.count"="1");

LOAD DATA LOCAL INPATH '/input_home/airbnb.csv' OVERWRITE INTO TABLE airbnb;
LOAD DATA LOCAL INPATH '/input_home/fueleconomy.csv' OVERWRITE INTO TABLE fueleconomy;
LOAD DATA LOCAL INPATH '/input_home/coil2000.csv' OVERWRITE INTO TABLE coil2000;
LOAD DATA LOCAL INPATH '/input_home/nba.csv' OVERWRITE INTO TABLE nba;
LOAD DATA LOCAL INPATH '/input_home/airbnb_incomplete.csv' OVERWRITE INTO TABLE airbnb_incomplete;
LOAD DATA LOCAL INPATH '/input_home/fueleconomy_incomplete.csv' OVERWRITE INTO TABLE fueleconomy_incomplete;
LOAD DATA LOCAL INPATH '/input_home/coil2000_incomplete.csv' OVERWRITE INTO TABLE coil2000_incomplete;
LOAD DATA LOCAL INPATH '/input_home/nba_incomplete.csv' OVERWRITE INTO TABLE nba_incomplete;
